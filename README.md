# How to Run the Code

## Download Required Packages
`pip install requirements.txt`

## Running the code on a limited subset
- Training the model using all permutations of the data would take way too long for the full dataset so we have reduced the 3000 size original data to just 100.
- Go to `CS184a_Final.ipynb` and click the `Run All` command at the top